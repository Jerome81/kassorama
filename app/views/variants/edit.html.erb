<div class="max-w-2xl mx-auto py-8">
  <div class="flex items-center mb-6">
     <%= link_to "← Back to Article", article_path(@variant.article), class: "text-gray-500 hover:text-[#FFD100] mr-4" %>
     <h1 class="text-2xl font-bold">Edit Variant: <%= @variant.name %></h1>
  </div>
  <div class="bg-white shadow rounded-lg p-6">
    <%= render 'form', variant: @variant %>
  </div>
</div>
