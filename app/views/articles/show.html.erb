<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render @article %>

    <div class="mt-4">
      <%= link_to "← Zurück zur Übersicht", articles_path, class: "text-gray-500 hover:text-gray-700 underline" %>
    </div>
  </div>
</div>
